{
  "manifest_version": 2,
  "name": "One Toolbar Shortcut",
  "version": "1.0",
  "description": "A toolbar button that takes you straight to one site you choose. Right-click the icon to set or change the destination.",
  "permissions": ["storage", "menus", "tabs", "activeTab"],
  "optional_permissions": ["*://*/*"],
  "browser_specific_settings": {
    "gecko": {
      "data_collection_permissions": {
        "required": ["none"]
      }
    }
  },
  "background": {
    "scripts": ["background.js"]
  },
  "browser_action": {
    "default_title": "Go to your pinned site (right-click to set)"
  },
  "options_ui": {
    "page": "options/options.html",
    "open_in_tab": false
  }
}
